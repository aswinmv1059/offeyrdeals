import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.offyrdeals.app',
  appName: 'OffyrDeals',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
