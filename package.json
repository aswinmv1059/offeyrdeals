{
  "name": "offyrdeals",
  "version": "1.0.0",
  "description": "Premium Offer Aggregator Platform for Kochi",
  "main": "index.html",
  "scripts": {
    "start": "npx serve .",
    "build": "echo 'Building...'",
    "android:init": "npx cap init OffyrDeals com.offyrdeals.app --web-dir .",
    "android:add": "npx cap add android",
    "android:copy": "npx cap copy",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
